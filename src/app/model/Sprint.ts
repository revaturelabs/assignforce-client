export class Sprint {
  id: number;
  name: string;
  body: string;

  constructor(
    id: number,
    name: string,
    body: string,
  
  ) {
    this.id = id;
    this.name = name;
    this.body = body;
  }
}
